# Only run once before running other files to import all 
# necessary libraries/frameworks
import os

# Install PyPDF2 to extract text from PDF files
os.system('pip install PyPDF2')

# Install pandas to handle CSV creation and manipulation (optional but useful)
os.system('pip install pandas')
